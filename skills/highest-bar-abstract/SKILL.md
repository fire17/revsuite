---
name: highest-bar-abstract
description: The HYBRID kickoff — /highest-bar and /ladder-abstraction activated as ONE skill. Runs the full highest-bar founding-vision protocol (verbatim VISION.md, activation ledger, budgets, dual showcases, swarm orchestration, darwin loops) WITH the ladder-of-abstraction doctrine fully enforced (twelve laws, typed LADDER.md rung specs, acceptance gates, act-from-the-big-picture with undo). Use when starting a new project that must be both done at the highest bar ever AND carry revolutionary zoom-level UX; on /highest-bar-abstract or /hba, "highest bar with the ladder", "kickoff with abstraction enforced", or a founding vision demanding both.
argument-hint: "<the founding vision — project name + what it should be and do>"
---

# /highest-bar-abstract — both protocols, one activation

This is a composition skill: activating it means ACTIVATING BOTH constituent
skills, in order, as one protocol. Do not paraphrase them from memory.

1. **Activate `/highest-bar`** (Skill tool, now). Its Phases 0–7 govern the whole
   kickoff: verbatim VISION.md + hash gate, the full stack activation ledger
   (/impeccable · /frontend-design:frontend-design · /wargame · /unknowns ·
   /mindblown-fast · /engineering-principles-pro · /master_engineering, plus
   /ponytail DEFERRED to builder subagents only — tmux mode, post-design),
   adjectives→budgets, showcase discipline, ≥12-agent orchestration, darwin loops.
2. **Activate `/ladder-abstraction`** (Skill tool, immediately after). Its Twelve
   Laws + Method are ENFORCED (not merely known): derive `LADDER.md` from the
   template, pass the a–g field-predicate gates before building any surface.
3. **Fusion rules:**
   - `LADDER.md` IS the information-architecture artifact highest-bar Phase 3
     asks about — one artifact, not two.
   - Ladder budgets live inside `BUDGETS.md` (one budget table for the project).
   - Both activations appear on the highest-bar activation ledger, each with ✅.
   - On any conflict: /impeccable's absolute bans win (ladder boundary rule);
     otherwise the stricter gate wins.

One-line composition contract: highest-bar sets the bar; ladder-abstraction sets
the information architecture; neither is optional here.
