---
name: mindblown-fast
description: Fast variant of /mindblown — runs the exact same AUTO SUPER HARNESS pipeline but
  fans out the execution phase via /pyramid (parallel opus lanes → verified sonnet subagents)
  for wall-clock speed. Use on /mindblown-fast, "blow my mind fast", "the ultimate way but
  parallel", "run the full stack in parallel". Same doctrine, pre-mortem, tracks, Gantry, the
  confidence gate (incl. deduped) and the /progress-report → /features-grid finish as
  /mindblown — only the execution phase differs.
---

# /mindblown-fast — the parallel AUTO SUPER HARNESS

Run the **`/mindblown`** skill exactly as written, with ONE change: the **execution phase
(step 3) fans out via `/pyramid`** — Fable main → opus lanes → ≤3 verified sonnet subagents,
every agent on an explicit non-Fable model (`workflow-model-guard`) — instead of a single
thread. Use when the work decomposes cleanly and tokens allow.

Everything else is identical: arm the doctrine → pre-mortem → `/tracks` → **Gantry** live
watch → the **confidence gate** (every principle met, verified for real, nothing missing,
**deduped** + leak-free + lean + fast) → bank & stage (`/dnl` · `/sas` · `/shipit`,
publish-ready never auto-publish) → the **show-off finish** (`/progress-report` →
`/features-grid`).

Full pipeline + hard rules + parked items: `~/.claude/skills/mindblown/SKILL.md`.
